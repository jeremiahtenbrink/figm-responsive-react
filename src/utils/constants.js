/**
 * @typedef UNITS
 * @type {{PERCENT: string, PX: string, EM: string, REM: string}}
 */
export const UNITS = {
  PX: "px", PERCENT: "%", REM: "rem", EM: "em", NONE: "NONE",
};

/**
 * @typedef CONSTRAINTS
 * @type {{CENTER: string, LEFT: string, TOP: string, RIGHT: string, BOTTOM:
 *   string}}
 */
export const CONSTRAINTS = {
  LEFT: "LEFT",
  TOP: "TOP",
  RIGHT: "RIGHT",
  BOTTOM: "BOTTOM",
  CENTER: "CENTER",
  SCALE: "SCALE",
  BOTH: "BOTH",
};